name: 'Hello World'
description: 'Greet someone'
inputs:
  platform:
    description: 'Target platform'
    required: true
    default: 'all'
  args:
    description: 'Arguments to be passed to the test command'
    required: false
# outputs:
#   random-number:
#     description: "Random number"
#     value: ${{ steps.random-number-generator.outputs.random-id }}
runs:
  using: "composite"
  steps:
    - run: cargo run --release --manifest-path=${{ github.action_path }} -- ${{ inputs.platform }} -- ${{ inputs.args }}
      shell: bash